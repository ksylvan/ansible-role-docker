---
# defaults file for docker
#
# Fedora 24 hack needed - See https://github.com/docker/docker/issues/23981
# Alternatively, you may set docker_selinux to permissive
#
docker_selinux: enforcing
#
# Use the testing repo instead of the main repo at yum.dockerproject.org
#
docker_testing: false
